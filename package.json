{
  "name": "buzz-in",
  "version": "1.0.0",
  "private": false,
  "description": "A trivia game where the score is the exact number of words the host never got to say.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "test": "node test/run.js",
    "check:copy": "node scripts/check-copy.js",
    "check:clues": "node scripts/check-clues.js",
    "check:offset": "node scripts/check-offset.js",
    "smoke": "node scripts/smoke.js",
    "verify": "npm run test && npm run check:copy && npm run check:clues && npm run check:offset"
  },
  "license": "MIT"
}
